import { Card } from "@models/card";
import React from "react";

interface HandCardProps {
	card: Card;
}

function HandCard({card}: HandCardProps) {
	return <div>HandCard</div>;
}

export default HandCard;
